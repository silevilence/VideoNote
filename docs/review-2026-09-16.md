# 2026-09-16 开发与审核记录

基线：f8ba2a2。范围：ROADMAP 开发中五项及用户确认的执行约定。每项按需求、质量、仓库约定快速审核，阻塞先修复再勾选。最终全量覆盖率和独立复核见后文。

## 1. 新建任务页

- 内联编辑保存独立快照，模板内容保持；空白使用默认报告要求；前后端必选模型、最大 16000 字符校验。
- multipart 元数据在前、视频流在后；禁用 MVC 表单值提供器，防止提前缓冲视频；保留原 octet-stream 接口并实施同样校验。
- 快速审核修复：默认表单绑定提前消耗请求流导致 500；现已禁用，三个新增用例通过。
- 验证：相关 51 项测试通过；Release 发布成功；Edge 浏览器通过上传、显式能力覆盖、内联快照、跳转、流式分析、结果持久化、列表、取消、轮询错误恢复与 390px 布局，无 pageerror。
- 结论：可原地勾选，无剩余阻塞。日志：work-tests/task1-test.log、task1-browser.log。

## 2. 任务列表与详情页

- 列表显示实时耗时；详情保存并展示阶段时间、状态与进度日志，取消和失败也保留。最终报告采用 Markdig Markdown，支持标题、列表、表格、代码块。
- 质量：禁用原始 HTML，移除危险链接协议，图片转为链接；日志与状态同次落库并复用终态保护。
- 快速审核修复：旧库新增日志列使用合法空数组默认值；重复进度不制造重复日志。
- 验证：9 项定向测试通过，消除测试空引用警告后相关 8 项复测通过；历史数据库迁移、安全渲染、日志顺序及状态保护均通过。Release/浏览器通过，实际标题渲染、日志、刷新、删除和移动端布局截图已检查。
- 结论：可原地勾选，无阻塞。日志：work-tests/task2-final.log、task2-browser.log。
- Markdown 依据：https://github.com/xoofx/markdig （DisableHtml 与语法树处理）。

## 3. Agent 对话

- Microsoft.Agents.AI 1.13.0 单 ChatClientAgent 复用模型工厂；每次请求一条完整材料上下文，随后注入已保存历史和本轮问题。UTF-8 字节保守估算预算，超限明确拒绝，不截断。
- 设置页全局模型落库，空值回退任务模型；删除配置外键置空。NDJSON 流式响应，非流式模型回退全文显示；只有成功问答在同一事务成对保存。
- 质量：完成且有报告才启用；同任务并发问答和生成中删除被拒绝；失败、空回答、长度中断、内容过滤、超时不保存；上游错误只记录类型，不泄露响应正文。
- 验证：15 项新增集成测试全部通过，覆盖两协议、流式/非流式、上下文计数、历史顺序、模型切换与删除、超时、并发、预算。Release 构建及浏览器通过流式可见、刷新历史、停止回复、未完成任务禁用、模型设置保存恢复。
- 结论：可原地勾选，无剩余阻塞。真实 DeepSeek 对话与完整链路统一在第 5 项执行并计数。全量回归及覆盖率在最终审核记录。
- SDK 依据：https://learn.microsoft.com/en-us/dotnet/api/microsoft.agents.ai.chatclientagent.runstreamingasync?view=agent-framework-dotnet-latest 及已安装 1.13.0 XML 文档。

## 4. 配置与部署文档

- 新增 README 的 Windows 开发/发布步骤、样例视频流程、测试命令与分层说明；配置文档逐项覆盖存储、FFmpeg、上传、分析预算、转写和对话默认值及约束。
- 明确模型配置位于数据库、转写选择优先级、环境变量/密钥环、备份恢复、单实例及外部访问控制边界；appsettings 显式列出原先仅在代码中的默认配置。AGENTS 同步实际能力与维护约定。
- 验证：Release 发布到新目录，以 Production 和随机端口启动独立实例，在新的空数据目录检查自动迁移、四个模板、上传限制、页面与模型默认值；保存全局对话模型后重启并确认保留。测试只启动/终止自己的进程。
- 限制：这是同一 Windows 开发机上的全新发布/数据目录验证，不是另装 Windows 虚拟机；SDK、Runtime 和 FFmpeg 作为 README 前置条件由本机提供。
- 快速审核未发现文档与实现不一致；可原地勾选。日志：work-tests/task4-publish.log、task4-startup.log。
- 此时全量 160 项通过、0 失败/跳过；行覆盖 1836/1982 = 92.63%，分支 852/1026 = 83.04%。WASM 由浏览器单独验证。

## 5. 端到端验收与打磨

- 四组协议集成链路（三模式，抽帧含音频能力两分支）延伸至 Agent 回复和历史读取；浏览器三模式均通过上传、进度、Markdown 报告、问答、刷新历史。
- 错误路径：无软字幕且无音轨、转写 HTTP 400、无匹配模型、取消任务均显示明确提示；未完成任务不显示可用对话输入。补充创建配置加载/重试和对话历史重新加载。
- 质量修复：历史加载失败后明确关闭发送并保留重试入口；Markdown 自动链接也应用协议白名单。测试脚本最初转写错误文案断言与实现不符，修正为实际提示后完整复测通过。
- 320 MiB 视频改用真实 multipart 流式入口验证，落盘大小与 SHA-256 一致，删除清理媒体与消息；相关测试通过。模式/转写/能力/后台相关 31 项与 Markdown 5 项定向回归通过。
- 真实 DeepSeek 使用现场生成的 8 秒图像/音轨/字幕，抽帧、字幕各 1 次分段 + 1 次报告 + 1 次 Agent，共 **6 次物理 HTTP 请求 / 20 次授权上限**。探针传输层持久化计数并禁止 SDK 自动重试。两模式 Agent 分别收到 65/52 个文本事件，每项持久化 2 条消息；人工核对回答确实引用测试图或合成字幕及相应时间范围。转写仍使用协议模拟。
- 真实结果位于忽略目录 work-tests/live-pipeline 的报告、conversation Markdown 和 request-count.txt；无密钥或原始上游错误写入记录。日志：work-tests/task5-live.log。
- 最终 Release 浏览器完整通过，包括对话停止、历史加载恢复、配置持久化、元数据重试、路由隔离、错误/通知保留、删除和 390px 视口，无 pageerror。桌面及移动截图已检查。日志：work-tests/task5-final-browser.log。
- 结论：可原地勾选，无剩余阻塞。完整审核与全量最终结果见下节。
