# 🗺️ Product Roadmap

## 📝 计划中 (Planned)

## 🚧 开发中 (In Progress)

本轮执行约定（2026-09-16，用户已确认）：按下面五项顺序开发、快速审核、修复阻塞、原地勾选并本地提交，最后完整审核。提示词内联编辑保存为本次任务快照，不修改模板；创建时必须选择模型。设置页保存全局对话模型，未指定则使用任务分析模型；完整报告与全部分段文本一次注入每次请求，不重复加入历史，超出上下文时明确拒绝而不截断；只有成功的完整问答成对保存。Gemini 视频及转写采用本地协议模拟，DeepSeek 抽帧、字幕及对话采用合成内容真实验证，总计最多 20 次模型请求；服务不可用时如实记录验收阻塞。测试使用独立数据库和服务。保留当前 ROADMAP 调整，在 main 本地提交，最终审核基线 f8ba2a2。

- [x] **新建任务页**（任务创建完整交互）
    - [x] 视频上传控件（含选择文件、上传进度）、模式选择（直接/抽帧/字幕）、模型选择（能力过滤）、提示词选择与内联编辑
    - [x] 提交入队并跳转任务详情
    - 验收：一次完整创建流程可提交任务且出现在任务列表，表单校验有效

- [ ] **任务列表与详情页**（任务进展与结果展示）
    - [ ] 任务列表：文件名、模式、状态、进度、耗时、创建时间，实时更新
    - [ ] 任务详情：阶段进度、分步日志、最终报告渲染（Markdown），完成前实时刷新
    - [ ] 失败态显示错误信息，支持删除任务
    - 验收：列表中任务状态随时间正确流转，详情页报告完成即显示，删除后列表移除

- [ ] **Agent 对话**（基于理解结果的问答助手）
    - [ ] 集成 Microsoft.Agents.AI：单 Assistant Agent，任务完成后于详情页启用对话区
    - [ ] 上下文注入：最终报告 + 全部分段理解文本，一次注入
    - [ ] 流式消息回复，对话消息持久化到数据库（刷新/重进后历史保留）
    - [ ] 对话模型可在设置中指定（默认取任务分析模型）
    - 验收：对已完成任务提问可得到基于视频内容的回答（流式），历史记录刷新后保留，未完成任务对话区不可用

- [ ] **配置与部署文档**（可运维性收尾）
    - [ ] appsettings 配置项说明（FFmpeg 路径、工作目录、转写与模型默认值）
    - [ ] 密钥管理方式说明（不硬编码）
    - [ ] README：功能说明、启动步骤、样例视频使用方式
    - 验收：按 README 在干净环境（同系统）可完成部署启动，配置项有注释或文档说明

- [ ] **端到端验收与打磨**（整体质量收口）
    - [ ] 三种模式完整链路冒烟：上传 → 进度 → 报告 → 对话
    - [ ] 错误路径处理：无字幕视频、无匹配模型、转写失败、任务取消
    - [ ] 加载态/空态/异常提示完善
    - 验收：主流程无阻断问题，已识别错误路径均有明确提示不崩溃

## ✅ 已完成 (Completed)

- [x] **搭建解决方案骨架**（Blazor Web App + 分层类库 + 基础设施就绪）
    - [x] 使用 Blazor Web App 模板（.NET 10，Interactive WebAssembly render mode）创建项目
    - [x] Client / Server / Shared 三层类库结构，服务端承载 API 与 SignalR
    - [x] 引入 EF Core + SQLite，创建本地工作目录约定（视频、帧、音频、字幕产物）
    - 验收：`dotnet run` 可启动，主页正常渲染，三层引用关系清晰构建通过

- [x] **设计数据模型与数据库初始化**（核心实体落库）
    - [x] 实体：提供商（Provider）、模型配置（ModelConfig）、分析任务（AnalysisTask）、提示词模板（PromptTemplate）、对话消息（ConversationMessage）
    - [x] 字段覆盖：协议类型、能力标记（思考/工具/流式/图像/音频/视频）、上下文窗口、任务状态、进度、结果文本、提示词内容
    - [x] 初始 EF Core Migration 成功生成
    - 验收：`dotnet ef database update` 创建 SQLite 库，各表结构与字段完整，基础 CRUD 冒烟通过

- [x] **提供商与模型配置 API**（两级配置的服务端接口）
    - [x] 提供商 CRUD：名称、协议类型（openai-compatible / gemini-native）、BaseUrl、ApiKey、可选转写模型
    - [x] 模型 CRUD：所属提供商、ModelId、能力标记全字段、上下文窗口
    - [x] 模型列表按提供商筛选，能力标记可任意组合编辑
    - 验收：REST API 增删改查全通，能力标记字段存取无损，ApiKey 不随列表接口明文返回

- [x] **模型设置管理 UI**（设置页：提供商与模型两级管理界面）
    - [x] 提供商列表/新建/编辑/删除表单
    - [x] 提供商下模型列表与新建/编辑/删除表单（含能力标记勾选、上下文窗口输入）
    - 验收：界面完成两级增删改查，刷新后数据保留，删除提供商时其模型级联处理行为明确

- [x] **AI 客户端工厂**（Microsoft.Extensions.AI 统一模型交互）
    - [x] 基于 IChatClient 抽象：OpenAI 兼容适配器与 Gemini 原生适配器
    - [x] 按数据库配置动态构建客户端（协议/地址/密钥/模型名）
    - [x] 流式响应（GetStreamingResponseAsync）与多模态输入能力透出
    - 验收：同一调用方代码可分别调通 OpenAI 兼容端点与 Gemini 端点，文本（流式与非流式）请求返回正确结果

- [x] **FFmpeg 服务封装**（视频物料处理能力）
    - [x] 视频分段：按固定时长切段（默认 60s，含 5s 重叠），输出各段基本信息（起止时间）
    - [x] 抽帧：按固定帧率抽帧（默认约 1fps），帧文件带时间戳标记
    - [x] 音频提取：输出标准音频文件（wav/mp3）
    - [x] 软字幕提取：内嵌字幕流提取为 srt
    - 验收：对样例视频运行各操作，产出文件正确（段数/帧数/时长/字幕内容校验），测试视频在仓库内可复现

- [x] **文件上传与存储**（视频上传落盘）
    - [x] 客户端流式上传视频到服务端（不占 WASM 全量内存）
    - [x] 服务端落盘到本地工作目录，校验扩展名与大小上限
    - [x] 任务删除时清理关联文件
    - 验收：数百 MB 视频上传成功且落盘完整，非法扩展名/超限被拒

- [x] **提示词模板**（理解重点的模式化配置）
    - [x] 内置模板：大纲提取、重点提取、摘要、关键词
    - [x] 模板 CRUD API 与管理 UI（新建/编辑/删除/复制内置模板）
    - 验收：模板增删改查可用并持久化，任务创建时可选用任意模板

本轮验收约定（2026-09-10）：Gemini 视频理解与音频转写使用本地协议模拟端点验收；DeepSeek 抽帧与字幕理解使用真实模型验证。每项按开发、快速审核、修复阻塞项、原地勾选、本地提交执行。

- [x] **后台任务框架与实时进度**（异步长任务执行通道）
    - [x] Channel 队列 + BackgroundService 串行消费（预留并发配置）
    - [x] 分析任务状态机：排队 → 预处理 → 理解 → 组合 → 完成/失败/取消，进度百分比与阶段描述
    - 验收：入队即显示排队，进度事件随阶段实时推送，取消任务生效，失败状态可见且有错误信息

- [x] **字幕转写服务**（无字幕视频的文本化）
    - [x] 封装 OpenAI 兼容 `/audio/transcriptions` 端点调用
    - [x] 转写模型可配置（提供商配置或全局配置），输入音频文件输出带时间戳文本
    - [x] 预留本地 Whisper 后端接口（v1 不实现）
    - 验收：对样例音频调用配置的转写端点返回正确字幕文本，失败时有明确错误

- [x] **三种模式预处理管线**（固定流程的视频物料准备）
    - [x] 直接理解：整视频按段切分，输出分段视频文件清单
    - [x] 抽帧理解：抽帧 + 提取音频；按所选模型音频能力决定音频直传或调用转写服务
    - [x] 字幕理解：优先提取内嵌软字幕，无字幕则调用转写服务
    - [x] 预处理进度细分反馈到 SignalR
    - 验收：三种模式分别对样例视频产出正确物料（帧/段/字幕/转写文本），阶段进度可见

- [x] **分段理解与报告组合**（Map-Reduce 核心理解管线）
    - [x] 逐段/逐帧组/逐字幕段调用模型生成中间理解文本（分段依据：模型上下文窗口与模式物料）
    - [x] 组合阶段：汇总各段理解，结合用户提示词生成最终报告（大纲/重点等按提示词产出）
    - [x] 理解阶段的流式文本通过 SignalR 推送
    - 验收：三种模式端到端输出完整报告（真实模型验证），流式推送客户端可见，分段与组合调次符合预期

- [x] **模型能力匹配规则**（模式与模型能力的适配）
    - [x] 规则：直接理解需视频能力、抽帧理解需图像能力、字幕理解任意文本模型；音频直传需音频能力
    - [x] 新建任务页按模式自动过滤候选模型，允许主动覆盖并提示
    - 验收：三种模式下列出候选集符合能力规则，手动覆盖可用且提交校验不拦阻显式选择
