# Change Log

## V0.1.1

### 🐛 Bug 修复

- 修复容器部署下首页空白的问题：此前发布的 `0.1.0` 镜像缺少 Blazor 启动脚本，页面静态资源请求返回 404，只显示背景。修复后镜像构建会检查启动脚本，缺失即构建失败。需要更新镜像才能恢复：重新拉取 `latest`，或把部署固定到 `0.1.1`；重复拉取 `0.1.0` 不会改善。

## V0.1.0

### ✨ 新功能

- 上传视频即可生成 AI 结构化解读报告，支持直接理解、抽帧理解、字幕理解三种模式；无内嵌字幕的视频会自动把音轨转写为带时间戳文本再解读。视频以流式方式上传，数百 MB 素材也可稳定落盘。
- 分析任务在后台队列中顺序执行，实时展示排队、预处理、理解、组合各阶段的进度、耗时、分步日志与流式生成文本，可随时取消；删除任务时一并清理本地物料。
- 任务完成后可就视频内容与 AI 对话问答：回答流式输出，成功问答成对持久保存；对话模型可在设置中指定，默认沿用该任务的分析模型。材料超出模型上下文窗口时明确拒绝，不静默截断。
- 提供商与模型两级配置管理，支持 OpenAI 兼容与 Gemini 原生两种协议，可标记模型能力（思考、工具、流式、图像、音频、视频）与上下文窗口。首次启动预置 DeepSeek 提供商，密钥仅通过环境变量引用，界面与接口不回显明文。
- 新建任务时按所选模式自动筛选可用模型，也可手动指定；提示词可选用内置模板（大纲、重点、摘要、关键词），支持内联改写，且仅对本次任务生效。
- 解读报告以安全 Markdown 渲染，屏蔽原始 HTML 与危险链接。
- 除本机 Windows 运行外，新增容器部署方式：提供镜像构建文件与单服务 Compose 部署示例，镜像内置 .NET 运行时与 FFmpeg，页面、接口与实时推送共用同一端口；数据库、密钥环与媒体物料集中在同一个可挂载的数据目录，密钥只从环境变量注入，宿主机端口与绑定地址可配置。示例限定单实例运行，不可横向扩副本共用同一数据库。
- 全新暗色主题界面与应用图标，附配置与运维说明、Windows 发布步骤与容器部署文档。
- 新增版本 Tag 自动发布：推送 `V0.1.0` 形式的 tag（大小写不敏感）后，自动校验对应版本更新记录、发布 GHCR 版本镜像并创建 GitHub Release；缺少更新记录时立即停止发布。
- 发布后补充：镜像提供 `latest` 标签，后续正式发布自动同步；Compose 默认使用 `latest`，仍可指定固定版本。支持为已发布镜像补标签，无需重新构建。
